<link rel="stylesheet" href="/stylesheets/partials.css" />
<!-- 
<nav>
    <% if (user) { %>
        <a href="/users/<%= user._id %>/jobs">Your Apps</a>
        <a href="/auth/sign-out">Sign Out</a>
    <% } else { %>
        <a href="/auth/sign-in">Sign In</a>
        <a href="/auth/sign-up">Sign Up</a>
    <% } %>
</nav> -->

<nav>
    <% if(user) { %>
    <div class="navbar">
      <h1 id="nav-header">iHelp</h1>
      <div class="nav-links">
        <a class="authlink" href="/users/<%=user._id%>/jobs">Your Jobs</a>
        <a class="authlink" href="/auth/sign-out">Sign Out</a>
      </div>
    </div>
    <% } else { %>
    <div class="navbar">
      <h1 id="nav-header">iHelp</h1>
      <div class="nav-links">
        <a class="authlink" href="/auth/sign-in">Sign In</a>
        <a class="authlink" href="/auth/sign-up">Sign Up</a>
      </div>
    </div>
    <% } %>
  </nav>
  